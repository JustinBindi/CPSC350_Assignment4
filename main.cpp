#include "Assignment4.cpp"
#include <iostream>
#include <vector>

using namespace std;

int main (int argc, char **argv)
{
  Simulation assignment;
  assignment.Run();



  /*for (int i = 0; i < 5; i++)
  {
    s1.defineStudent(i,i,i);//,s2,s3,s4,s5;
    studentVector.push_back(s1);
    s1.printInfo(studentVector[i]);

    /*Student s2(1,2,3);
    Student s3(1,2,3);
    Student s4(1,2,3);
    Student s5(1,2,3);*/
  //}

  /*s1.defineStudent(1,23,5);
  studentVector.push_back(s2);
  studentVector.push_back(s3);
  studentVector.push_back(s4);
  studentVector.push_back(s5);

  for (int i = 0; i < 5; i++)
  {
    //string s = "s";
    //s += to_string(i);
    s1.printInfo(studentVector[i]);
    //cout << to_string(studentVector[i]) << endl;
  }*/

  /*myQueue.insert('R');
  myQueue.insert('E');
  myQueue.insert('N');
  myQueue.insert('E');

  cout << "remove :" << myQueue.remove() << endl;
  cout << "peek :" << myQueue.front() << endl;

  myQueue.insert('F');
  cout << "peek :" << myQueue.front() << endl;

  while (!myQueue.isEmpty())
  {
    cout << "removing :" << myQueue.remove() << endl;
  }*/
  return 0;
}
